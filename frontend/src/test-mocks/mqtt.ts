// Stub for the mqtt Node.js package.
// The shared types.ts imports IClientOptions from 'mqtt' (type-only usage).
// This stub allows Vite to resolve the import in the frontend test context.
export interface IClientOptions {}
